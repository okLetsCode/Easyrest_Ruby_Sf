Environment Setup

Global Dependencies
Install Ruby (sudo apt install ruby-full)
Install bundler
$ sudo gem install bundler

Project Dependencies
Install packages
$ sudo bundle install
Running Tests
$ rspec
